# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'
plugin 'cocoapods-art', :sources => [
  'cocoapods-identy-face'
]

target 'NFCSelfie' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

   pod 'IdentyFace','4.2.0'
   pod 'IdentyFaceLocal','4.2.0'
   pod 'OpenSSL-Universal', :path => '/Users/danielgaribay/Desktop/OpenSSL/OpenSSL'
   pod 'DocumentReader'
   pod 'DocumentReaderFullRFID'

end
